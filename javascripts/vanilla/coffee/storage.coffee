localStorage.setItem "favorites", "[]"  unless localStorage["favorites"]
localStorage["seller"] = 12  if not localStorage["seller"] or localStorage["seller"] is "undefined"
localStorage["category"] = 0  if not localStorage["category"] or localStorage["category"] is "undefined"
